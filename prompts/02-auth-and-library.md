# Codex Prompt 02 — Accounts and project library

Implement Milestone 02 from `TASKS.md` on top of the completed canonical course model.

Requirements:
- teacher registration/login/logout;
- strong password hashing;
- revocable session/refresh-token strategy;
- server-side ownership enforcement;
- project library UI: create, open, rename, duplicate, archive/delete;
- opening an existing project restores canonical course state;
- add tests proving one teacher cannot read/update another teacher's project.

Do not add AI provider work in this milestone. Run tests and update docs before finishing.
